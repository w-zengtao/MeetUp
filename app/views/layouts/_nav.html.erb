<nav class="top-nav navbar navbar-default row">
  <div class="container-fluid">
    <div class="navbar-header">
      <a class="navbar-brand" href="#" style="padding-left: 2em">Ztao</a>
    </div>
    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav navbar-right">
        <li>
          <%= link_to "首页", root_path %>
        </li>
        <% if current_user %>
          <li class='top-nav-item' id='navto.about'>
            <a><%= current_user.name %></a>
          </li>
          <li>
            <%= link_to "登出", session_path(current_user), method: :delete %>
          </li>
        <% else %>
          <li>
            <%= link_to "注册", new_user_path %>
          </li>
          <li>
            <%= link_to "登录", new_session_path %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
